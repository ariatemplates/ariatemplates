resources:
 '/':
  - 'src'
 '/test':
  - 'test'
tests:
 aria-templates:
  extraScripts:
    - /aria/css/atskin.js
  classpaths:
   includes:
    - test.MainTestSuite
   excludes:
   #Excluded because PhantomJS does not support flash:
    - test.aria.core.io.IOXDRTest
   #Excluded because vertical align  is not revelant in PhantomJS
    - test.aria.widgets.verticalAlign.VerticalAlignTestCase
